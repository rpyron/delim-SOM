% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026_04_07_delim-SOM_2.0_functions.R
\name{plot.K.SOM}
\alias{plot.K.SOM}
\title{Plot support for alternative numbers of SOM clusters (K)}
\usage{
\method{plot}{K.SOM}(
  SOM.output,
  col.pal = viridis::magma,
  save = FALSE,
  overwrite = TRUE,
  plot.type = "svg",
  file.name = NULL,
  width = 10,
  height = 15,
  resolution = 300,
  bottom.margin = 0.5,
  left.margin = 0.5,
  top.margin = 2,
  right.margin = 0,
  plot.title = "auto",
  plot.title.font.size = 9.1,
  axis.labels.font.size = 9.1,
  axis.ticks.font.size = 7
)
}
\arguments{
\item{SOM.output}{A list returned by \code{clustering.SOM}. The object must contain
\code{N_replicates}, \code{optim_k_vals}, and \code{max_k}. If available,
\code{support_values}, \code{support_label}, and \code{BIC_values} are used to create the
support-value and delta-BIC panels.}

\item{col.pal}{A viridis color-palette function used to assign colors to
candidate K values. Supported palettes are \code{viridis::viridis},
\code{viridis::magma}, \code{viridis::plasma}, \code{viridis::inferno},
\code{viridis::cividis}, \code{viridis::rocket}, \code{viridis::mako}, and
\code{viridis::turbo}. Default: \code{viridis::magma}.}

\item{save}{Logical; if \code{TRUE}, the plot is saved to a file. Default:
\code{FALSE}.}

\item{overwrite}{Logical; if \code{TRUE}, an existing output file with the same
name is overwritten when \code{save = TRUE}. If \code{FALSE}, plotting is aborted
when the output file already exists. Default: \code{TRUE}.}

\item{plot.type}{A single character string specifying the file format when
\code{save = TRUE}. Supported values are \code{"svg"}, \code{"png"}, and \code{"jpg"}.
Default: \code{"svg"}.}

\item{file.name}{Optional character string giving the output file name when
\code{save = TRUE}. If \code{NULL}, the default file name is
\code{"K_plot.<plot.type>"}. Default: \code{NULL}.}

\item{width}{A single positive numeric value giving the plot width in
centimeters when \code{save = TRUE}. Default: \code{10}.}

\item{height}{A single positive numeric value giving the plot height in
centimeters when \code{save = TRUE}. Default: \code{15}.}

\item{resolution}{A single numeric value giving the plot
resolution in dpi for \code{"png"} and \code{"jpg"} output. Default: \code{300}.}

\item{bottom.margin}{A single non-negative numeric value giving the bottom
outer plot margin in lines. Default: \code{0.5}.}

\item{left.margin}{A single non-negative numeric value giving the left outer
plot margin in lines. Default: \code{0.5}.}

\item{top.margin}{A single non-negative numeric value giving the top outer
plot margin in lines. Default: \code{2}.}

\item{right.margin}{A single non-negative numeric value giving the right outer
plot margin in lines. Default: \code{0}.}

\item{plot.title}{Optional character string giving the main plot title. If
\code{"auto"}, the title is generated from the support measure. If \code{NULL}, no
title is shown. Default: \code{"auto"}.}

\item{plot.title.font.size}{A single positive numeric value giving the plot
title font size in points. Default: \code{9.1}.}

\item{axis.labels.font.size}{A single positive numeric value giving the
y-axis-title and bottom x-axis K-label font size in points. Default: \code{9.1}.}

\item{axis.ticks.font.size}{A single positive numeric value giving the
y-axis numeric tick-label font size in points. Default: \code{7}.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its plotting
side effect. If \code{save = TRUE}, the plot is also written to the specified
file.
}
\description{
Plot replicate-level support for alternative numbers of clusters from a
clustered Self-Organizing Map (SOM). Depending on the available clustering
results, the function plots support values across candidate K values,
successive delta-BIC values, and the sampling frequency with which each K
value was selected across retained SOM replicates.
}
\details{
\code{plot.K.SOM} visualizes replicate-level support for alternative numbers of
clusters, thus summarizing the results returned by \code{clustering.SOM}. The plots
show how support changes across candidate K values and whether replicate maps
consistently select one delimitation level or support several alternative
values of K.

The support-value panel shows boxplots of the finite support values stored for
each candidate K across retained SOM replicates. Depending on the clustering
method used in \code{clustering.SOM}, these values may represent BIC-like criteria,
Davies-Bouldin indices, or silhouette support. Candidate K values without any
finite support values are omitted from this panel. The y-axis title is taken
from \code{support_label} when available. For all BIC-based clustering methods,
smaller stored values indicate better support.

When BIC values are available and \code{max_k} is at least 3, an additional
delta-BIC panel shows the improvement between each successive pair of
candidate values. For each replicate, delta BIC for a candidate K is
calculated as the BIC value for K - 1 minus the BIC value for K. Positive
values indicate improvement in support for the higher K, whereas values
near or below zero indicate little or no improvement. If no finite
successive delta-BIC values are available, the delta-BIC panel is omitted.

The bottom panel shows the frequency of each selected K among retained
replicates. If no finite support curve is available, as for \code{"HDBSCAN"},
only the K-frequency panel is shown.

If \code{file.name = NULL}, the default file name is \code{"K_plot.<plot.type>"}.
}
\examples{
\dontrun{
set.seed(1)

# Create multi-layer example data
snp_data <- matrix(sample(0:2, 50 * 20, replace = TRUE),
                   nrow = 50,
                   ncol = 20)
morphology_data <- matrix(rnorm(50 * 5), nrow = 50, ncol = 5)
environment_data <- matrix(rnorm(50 * 4), nrow = 50, ncol = 4)
rownames(snp_data) <- paste0("sample_", seq_len(nrow(snp_data)))
rownames(morphology_data) <- rownames(snp_data)
rownames(environment_data) <- rownames(snp_data)
input_data_multi <- list(
  SNPs = snp_data,
  Morphology = morphology_data,
  Environment = environment_data
)

# Train multi-layer SOM
som_multi <- train.SOM(input_data = input_data_multi)

# Cluster SOM
som_clustered <- clustering.SOM(
  SOM.output = som_multi,
  clustering.method = "kmeans+BICthreshold"
)

# Plot K evaluation
plot.K.SOM(SOM.output = som_clustered)

# Save K-evaluation plot
plot.K.SOM(
  SOM.output = som_clustered,
  save = TRUE,
  plot.type = "svg",
  file.name = "SOM_K_plot.svg"
)
}

}
