% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026_04_07_delim-SOM_2.0_functions.R
\name{plot.structure.SOM}
\alias{plot.structure.SOM}
\title{Plot Structure-like SOM assignment coefficients}
\usage{
\method{plot}{structure.SOM}(
  SOM.output,
  col.pal = viridis::viridis,
  save = FALSE,
  overwrite = TRUE,
  plot.type = "svg",
  file.name = NULL,
  width = 16,
  height = 10,
  resolution = 300,
  bottom.margin = 5,
  left.margin = 5,
  top.margin = 1.5,
  right.margin = 1.5,
  Individual.labels.font.size = 7,
  Y.axis.title = "Cluster assignment coefficient",
  axis.labels.font.size = 9.1,
  axis.ticks.font.size = 7,
  sort.by.col = 1,
  bar.border.col = NULL,
  bar.border.lwd = 1,
  verbose = TRUE
)
}
\arguments{
\item{SOM.output}{A list returned by \code{clustering.SOM}. The object must contain
\code{ancestry_matrix}, a numeric sample-by-cluster matrix with at least two
samples and two clusters.}

\item{col.pal}{A viridis color-palette function used to assign colors to
clusters. Supported palettes are \code{viridis::viridis}, \code{viridis::magma},
\code{viridis::plasma}, \code{viridis::inferno}, \code{viridis::cividis},
\code{viridis::rocket}, \code{viridis::mako}, and \code{viridis::turbo}. Default:
\code{viridis::viridis}.}

\item{save}{Logical; if \code{TRUE}, the plot is saved to a file. Default:
\code{FALSE}.}

\item{overwrite}{Logical; if \code{TRUE}, an existing output file with the same
name is overwritten when \code{save = TRUE}. If \code{FALSE}, plotting is aborted
when the file already exists. Default: \code{TRUE}.}

\item{plot.type}{A single character string specifying the file format when
\code{save = TRUE}. Supported values are \code{"svg"}, \code{"png"}, and \code{"jpg"}.
Default: \code{"svg"}.}

\item{file.name}{Optional character string giving the output file name when
\code{save = TRUE}. If \code{NULL}, a default name is generated from the SOM input
layer names and \code{plot.type}. Default: \code{NULL}.}

\item{width}{A single positive numeric value giving the plot width in
centimeters when \code{save = TRUE}. Default: \code{16}.}

\item{height}{A single positive numeric value giving the plot height in
centimeters when \code{save = TRUE}. Default: \code{10}.}

\item{resolution}{A single numeric value giving the plot
resolution in dpi for \code{"png"} and \code{"jpg"} output. Default: \code{300}.}

\item{bottom.margin}{A single non-negative numeric value giving the bottom
plot margin in lines. Default: \code{5}.}

\item{left.margin}{A single non-negative numeric value giving the left plot
margin in lines. Default: \code{5}.}

\item{top.margin}{A single non-negative numeric value giving the top plot
margin in lines. Default: \code{1.5}.}

\item{right.margin}{A single non-negative numeric value giving the right plot
margin in lines. Default: \code{1.5}.}

\item{Individual.labels.font.size}{A single positive numeric value giving the
sample-label font size in points. Sample labels are taken from the row names
of \code{ancestry_matrix}. Default: \code{7}.}

\item{Y.axis.title}{Optional character string giving the y-axis title. If
\code{NULL} or an empty string, no y-axis title is shown. Default:
\code{"Cluster assignment coefficient"}.}

\item{axis.labels.font.size}{A single positive numeric value giving the
y-axis-title font size in points. Default: \code{9.1}.}

\item{axis.ticks.font.size}{A single positive numeric value giving the
y-axis numeric tick-label font size in points. Default: \code{7}.}

\item{sort.by.col}{Optional positive integer specifying the first dominant
cluster in the sample ordering. If \code{NULL}, samples are ordered by
single-linkage hierarchical clustering of their complete assignment
profiles. Default: \code{1}.}

\item{bar.border.col}{Optional character string giving the color of vertical
separation lines between samples. If \code{NULL}, no separation lines are
drawn. Default: \code{NULL}.}

\item{bar.border.lwd}{Optional positive numeric value (or NULL) giving the
line width of vertical separation lines between samples. Ignored when
\code{bar.border.col = NULL}. Default: \code{1}.}

\item{verbose}{Logical; if \code{TRUE}, informative messages and warnings are
printed. Default: \code{TRUE}.}
}
\value{
Invisibly returns the numeric assignment-coefficient matrix displayed
in the plot. If \code{save = TRUE}, the plot is also written to the specified file.
}
\description{
Plot the replicate-consensus cluster assignment coefficients returned by
\code{clustering.SOM} as a Structure-like stacked barplot. Each vertical bar
represents one sample, and the stacked colored segments represent the
proportion of retained replicate maps assigning that sample to each inferred
cluster.
}
\details{
\code{plot.structure.SOM} visualizes the individual-level \code{ancestry_matrix}
returned by \code{clustering.SOM} as a Structure-like stacked barplot. This
presentation follows the general assignment-coefficient visualization widely
used in population-genetic clustering analyses (Pritchard et al., 2000).
Each row of \code{ancestry_matrix} represents one sample, each column represents
one inferred cluster, and each value gives the proportion of retained SOM
replicates in which the sample was assigned to that cluster. These values can
be interpreted as replicate-consensus "species coefficients" (Pyron, 2023;
Pyron et al., 2023). Samples dominated by one color are consistently assigned
to one cluster across replicate maps, whereas samples with intermediate
contributions from multiple colors are assigned to different clusters across
replicates. Such patterns may reflect uncertain delimitation, weak
differentiation, recent divergence, incomplete lineage sorting, hybridization,
or conflicting signals among data layers. The plot is not produced for K = 1
because all samples would have an assignment coefficient of one for the same cluster.

If \code{sort.by.col} is supplied, samples are grouped by their dominant cluster,
beginning with the specified cluster and continuing through the remaining
clusters in column order. Within each group, samples are ordered from highest
to lowest assignment coefficient for that cluster. For example, with three
clusters, \code{sort.by.col = 2} produces Cluster 2, Cluster 3, and Cluster 1. If
\code{sort.by.col = NULL}, Euclidean distances are calculated among complete
assignment profiles, and samples are ordered using single-linkage hierarchical
clustering. Cluster colors follow the column order of \code{ancestry_matrix}.

Sample names are displayed vertically below the bars. For datasets containing
many samples or long sample names, increasing \code{width} or \code{bottom.margin}, or
decreasing \code{Individual.labels.font.size}, may improve readability.

When saving is requested, the function supports SVG, PNG, and JPEG output. A
default file name is generated as
\code{"SOM_structure_plot_<input-layer-names>.<plot.type>"} when \code{file.name} is
\code{NULL}.
}
\examples{
\dontrun{
set.seed(1)

# Create multi-layer example data
snp_data <- matrix(sample(0:2, 50 * 20, replace = TRUE),
                   nrow = 50,
                   ncol = 20)
morphology_data <- matrix(rnorm(50 * 5), nrow = 50, ncol = 5)
environment_data <- matrix(rnorm(50 * 4), nrow = 50, ncol = 4)
rownames(snp_data) <- paste0("sample_", seq_len(nrow(snp_data)))
rownames(morphology_data) <- rownames(snp_data)
rownames(environment_data) <- rownames(snp_data)

# Train multi-layer SOM
som_multi <- train.SOM(
  input_data = list(
    SNPs = snp_data,
    Morphology = morphology_data,
    Environment = environment_data
  )
)

# Cluster SOM
som_clustered <- clustering.SOM(
  SOM.output = som_multi,
  clustering.method = "kmeans+BICelbow"
)

# Plot structure-style assignment coefficients
plot.structure.SOM(SOM.output = som_clustered)

# Save structure-style plot
plot.structure.SOM(
  SOM.output = som_clustered,
  save = TRUE,
  plot.type = "svg",
  file.name = "SOM_structure_plot.svg"
)
}

}
\references{
Pritchard, J. K., Stephens, M., & Donnelly, P. (2000). Inference of
population structure using multilocus genotype data. \emph{Genetics},
155(2), 945-959. https://doi.org/10.1093/genetics/155.2.945

Pyron, R. A. (2023). Unsupervised machine learning for species delimitation,
integrative taxonomy, and biodiversity conservation. \emph{Molecular
Phylogenetics and Evolution}, 189, 107939.
https://doi.org/10.1016/j.ympev.2023.107939

Pyron, R. A., O'Connell, K. A., Duncan, S. C., Burbrink, F. T., & Beamer,
D. A. (2023). Speciation hypotheses from phylogeographic delimitation yield
an integrative taxonomy for seal salamanders (\emph{Desmognathus
monticola}). \emph{Systematic Biology}, 72(1), 179-197.
https://doi.org/10.1093/sysbio/syac065
}
