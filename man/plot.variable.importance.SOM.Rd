% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026_04_07_delim-SOM_2.0_functions.R
\name{plot.variable.importance.SOM}
\alias{plot.variable.importance.SOM}
\title{Plot variable importance across SOM layers}
\usage{
\method{plot}{variable.importance.SOM}(
  SOM.output,
  mode = "Cluster.separation",
  col.pal = viridis::turbo,
  save = FALSE,
  overwrite = TRUE,
  plot.type = "svg",
  file.name = NULL,
  width = 16,
  height = 10,
  resolution = 300,
  bottom.margin.total = 2,
  left.margin.total = 1,
  top.margin.total = 2,
  right.margin.total = 0,
  bottom.margin = 2.5,
  left.margin = 3,
  top.margin = 2,
  right.margin = 2,
  bars.threshold.N = 50,
  x.axis.label = if (mode == "Cluster.separation")
    "Cluster separation (eta squared effect size)" else "Variance across SOM map",
  title.font.size = 9.1,
  layer.labels.font.size = 9.1,
  bar.label.font.size = 7,
  axis.labels.font.size = 9.1,
  axis.ticks.font.size = 7,
  add.boxplot.whiskers = TRUE,
  importance.threshold = 0.001,
  set.k = NULL
)
}
\arguments{
\item{SOM.output}{A list returned by \code{train.SOM} or \code{clustering.SOM}. The
object must contain \code{som_models} and \code{input_data_names}. For
\code{mode = "Cluster.separation"}, it must also contain replicate-specific
\code{som_clusters} returned by \code{clustering.SOM}.}

\item{mode}{A single character string specifying the variable-importance
metric. Supported values are \code{"Cluster.separation"} and \code{"Map.variance"}.
Default: \code{"Cluster.separation"}.}

\item{col.pal}{A viridis color-palette function used to assign colors to
input layers. Supported palettes are \code{viridis::viridis}, \code{viridis::magma},
\code{viridis::plasma}, \code{viridis::inferno}, \code{viridis::cividis},
\code{viridis::rocket}, \code{viridis::mako}, and \code{viridis::turbo}. Default:
\code{viridis::turbo}.}

\item{save}{Logical; if \code{TRUE}, the plot is saved to a file. Default:
\code{FALSE}.}

\item{overwrite}{Logical; if \code{TRUE}, an existing output file with the same
name is overwritten when \code{save = TRUE}. If \code{FALSE}, plotting is aborted
when the output file already exists. Default: \code{TRUE}.}

\item{plot.type}{A single character string specifying the file format when
\code{save = TRUE}. Supported values are \code{"svg"}, \code{"png"}, and \code{"jpg"}.
Default: \code{"svg"}.}

\item{file.name}{Optional character string giving the output file name when
\code{save = TRUE}. If \code{NULL}, a default name is generated from \code{mode}, the SOM
input layer names, and \code{plot.type}. Default: \code{NULL}.}

\item{width}{A single positive numeric value giving the plot width in
centimeters when \code{save = TRUE}. Default: \code{16}.}

\item{height}{A single positive numeric value giving the plot height in
centimeters when \code{save = TRUE}. Default: \code{10}.}

\item{resolution}{A single numeric value giving the plot resolution
in dpi for \code{"png"} and \code{"jpg"} output. Default: \code{300}.}

\item{bottom.margin.total}{A single non-negative numeric value giving the
bottom outer margin of the multi-panel plot in lines. Default: \code{2}.}

\item{left.margin.total}{A single non-negative numeric value giving the left
outer margin of the complete multi-panel plot in lines. Default: \code{1}.}

\item{top.margin.total}{A single non-negative numeric value giving the top
outer margin of the complete multi-panel plot in lines. Default: \code{2}.}

\item{right.margin.total}{A single non-negative numeric value giving the right
outer margin of the complete multi-panel plot in lines. Default: \code{0}.}

\item{bottom.margin}{A single non-negative numeric value giving the bottom
margin of each layer panel in lines. Default: \code{2.5}.}

\item{left.margin}{A single non-negative numeric value giving the left margin
of each layer panel in lines. Default: \code{3}.}

\item{top.margin}{A single non-negative numeric value giving the top margin of
each layer panel in lines. Default: \code{2}.}

\item{right.margin}{A single non-negative numeric value giving the right
margin of each layer panel in lines. Default: \code{2}.}

\item{bars.threshold.N}{A single non-negative integer giving the maximum
number of plotted variables for which variable labels and boxplot whiskers
are shown. This argument does not remove variables from the plot. Default:
\code{50}.}

\item{x.axis.label}{Optional character string giving the shared x-axis title.
If \code{NULL}, no shared x-axis title is shown. By default,
\code{"Cluster separation (eta squared effect size)"} is used for
\code{mode = "Cluster.separation"} and \code{"Variance across SOM map"} is used for
\code{mode = "Map.variance"}.}

\item{title.font.size}{A single positive numeric value giving the main plot
title font size in points. Default: \code{9.1}.}

\item{layer.labels.font.size}{A single positive numeric value giving the layer
title font size in points. Default: \code{9.1}.}

\item{bar.label.font.size}{A single positive numeric value giving the variable
label font size in points. Default: \code{7}.}

\item{axis.labels.font.size}{A single positive numeric value giving the shared
x-axis-title font size in points. Default: \code{9.1}.}

\item{axis.ticks.font.size}{A single positive numeric value giving the x-axis
numeric tick-label font size in points. Default: \code{7}.}

\item{add.boxplot.whiskers}{Logical; if \code{TRUE}, boxplot whiskers and staples
are shown when the number of plotted variables does not exceed
\code{bars.threshold.N}. Default: \code{TRUE}.}

\item{importance.threshold}{A single non-negative numeric value giving the
minimum median variable-importance value required for a variable to be
plotted. Variables with median importance less than or equal to this value
are omitted. Default: \code{0.001}.}

\item{set.k}{Optional single positive integer. For
\code{mode = "Cluster.separation"}, only SOM replicates with this number of
clusters are included. If \code{NULL}, all replicates with at least two clusters
are included. This argument is ignored for \code{mode = "Map.variance"}.
Default: \code{NULL}.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its plotting
side effect. If \code{save = TRUE}, the plot is also written to the specified
file.
}
\description{
Plot variable-level importance across SOM replicates for each input layer of
a trained SOM. Variable importance can be quantified as cluster separation
using eta squared effect size or as variance across the SOM map.
}
\details{
\code{plot.variable.importance.SOM} recalculates variable importance separately for
each retained SOM replicate using the layer-specific codebook vectors and
sample-to-unit assignments stored in \code{som_models}. The resulting
replicate-level distributions are displayed as horizontal boxplots for each
input layer.

For \code{mode = "Cluster.separation"}, variable importance is quantified using a
weighted eta-squared effect size, equivalent to an ANOVA-like effect size
(Cohen, 1973; Richardson, 2011). For each retained replicate, SOM units are
weighted by their number of mapped samples plus a baseline weight of one. This
gives greater influence to units representing more samples while allowing
empty units to retain a small contribution. Eta squared is calculated for each
variable as the weighted between-cluster sum of squares divided by the
weighted total sum of squares and therefore represents the proportion of
weighted variation associated with the inferred SOM-unit clusters. Larger
values indicate stronger separation among the inferred clusters. If \code{set.k}
is supplied, only replicates with that value of K are retained. Eta squared
cannot be calculated for K = 1, so the function returns without plotting if
all eligible replicates have K = 1.

This mode is the recommended default for assessing variable importance in
cluster differentiation. It is important to consider that eta squared
summarizes the overall separation among clusters and does not identify which
individual cluster or pair of clusters drives that separation. For example,
a large effect size may result because one cluster differs strongly from two
otherwise similar clusters, or because all clusters differ moderately from
one another.

For \code{mode = "Map.variance"}, variable importance is quantified as the weighted
variance of each codebook variable across SOM units. As in
\code{mode = "Cluster.separation"}, SOM units are weighted using their mapped-sample
counts plus a baseline weight of one. The weighted squared deviations from the
weighted mean are divided by the sum of the weights. Larger values indicate
stronger variation across the SOM surface and therefore a greater contribution
to its broader topological organization.

Variation across the map is necessary for a variable to contribute to cluster
differentiation, whereas a variable with little or no variation cannot
strongly differentiate clusters. Large map variance alone does not mean that
the variation is aligned with the inferred clusters, as also shown by our
testing. Therefore, this mode is intended as a complementary measure of
variable importance. If variables that are highly important under
\code{mode = "Cluster.separation"} also show large map variance, this provides
additional evidence for their importance.

In addition to its complementary role, \code{mode = "Map.variance"} is intended to
assess variable importance for cases with K = 1, for which the
\code{mode = "Cluster.separation"} metric cannot be calculated.

For each layer, the median importance of every variable is calculated across
retained replicates. Variables with median importance greater than
\code{importance.threshold} are retained and ordered from the smallest to the
largest median importance. Variables with median importance less than or equal
to the threshold are omitted. Variable labels are shown when the number of
retained variables in a layer does not exceed \code{bars.threshold.N}. Boxplot
whiskers and staples are shown only when \code{add.boxplot.whiskers = TRUE} and the
number of retained variables does not exceed this threshold. Boxplot outliers
are not displayed.

If \code{file.name = NULL}, the default file name is
\code{"SOM_etasquared_plot_<input-layer-names>.<plot.type>"} for
\code{mode = "Cluster.separation"} and
\code{"SOM_map_variance_plot_<input-layer-names>.<plot.type>"} for
\code{mode = "Map.variance"}.
}
\examples{
\dontrun{
set.seed(1)

# Create multi-layer example data
snp_data <- matrix(sample(0:2, 50 * 200, replace = TRUE),
                   nrow = 50,
                   ncol = 200)
morphology_data <- matrix(rnorm(50 * 5), nrow = 50, ncol = 5)
environment_data <- matrix(rnorm(50 * 4), nrow = 50, ncol = 4)
rownames(snp_data) <- paste0("sample_", seq_len(nrow(snp_data)))
rownames(morphology_data) <- rownames(snp_data)
rownames(environment_data) <- rownames(snp_data)

input_data_multi <- list(
  SNPs = snp_data,
  Morphology = morphology_data,
  Environment = environment_data
)

# Train multi-layer SOM
som_multi <- train.SOM(input_data = input_data_multi)

# Cluster SOM
som_clustered <- clustering.SOM(
  SOM.output = som_multi,
  clustering.method = "kmeans+BICthreshold"
)

# Plot cluster-separation importance
plot.variable.importance.SOM(
  SOM.output = som_clustered,
  mode = "Cluster.separation"
)

# Plot map-variance importance
plot.variable.importance.SOM(
  SOM.output = som_clustered,
  mode = "Map.variance"
)

# Save cluster-separation plot
plot.variable.importance.SOM(
  SOM.output = som_clustered,
  mode = "Cluster.separation",
  save = TRUE,
  plot.type = "svg",
  file.name = "SOM_variable_importance.svg"
)
}

}
\references{
Cohen, J. (1973). Eta-squared and partial eta-squared in fixed factor ANOVA
designs. \emph{Educational and Psychological Measurement}, 33(1), 107-112.
https://doi.org/10.1177/001316447303300111

Richardson, J. T. E. (2011). Eta squared and partial eta squared as measures
of effect size in educational research. \emph{Educational Research Review},
6(2), 135-147. https://doi.org/10.1016/j.edurev.2010.12.001
}
