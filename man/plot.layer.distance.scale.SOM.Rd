% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026_04_07_delim-SOM_2.0_functions.R
\name{plot.layer.distance.scale.SOM}
\alias{plot.layer.distance.scale.SOM}
\title{Plot SOM layer distance scales}
\usage{
\method{plot}{layer.distance.scale.SOM}(
  SOM.output,
  col.pal = viridis::turbo,
  save = FALSE,
  overwrite = TRUE,
  plot.type = "svg",
  file.name = NULL,
  width = 16,
  height = 10,
  resolution = 300,
  bottom.margin = 3,
  left.margin = 5,
  top.margin = 2.5,
  right.margin = 1,
  plot.title = "Layer distance scale across layers",
  plot.title.font.size = 9.1,
  y.axis.label = "Average pairwise distance",
  axis.labels.font.size = 9.1,
  axis.ticks.font.size = 7
)
}
\arguments{
\item{SOM.output}{A list returned by \code{train.SOM}. The object must contain
\code{distance_weights_matrix}, a numeric replicate-by-layer matrix containing
positive internal distance-normalization weights. At least two input layers
are required.}

\item{col.pal}{A viridis color-palette function used to assign colors to
input layers. Supported palettes are \code{viridis::viridis}, \code{viridis::magma},
\code{viridis::plasma}, \code{viridis::inferno}, \code{viridis::cividis},
\code{viridis::rocket}, \code{viridis::mako}, and \code{viridis::turbo}. Default:
\code{viridis::turbo}.}

\item{save}{Logical; if \code{TRUE}, the plot is saved to a file. Default:
\code{FALSE}.}

\item{overwrite}{Logical; if \code{TRUE}, an existing output file with the same
name is overwritten when \code{save = TRUE}. If \code{FALSE}, plotting is aborted
when the output file already exists. Default: \code{TRUE}.}

\item{plot.type}{A single character string specifying the file format when
\code{save = TRUE}. Supported values are \code{"svg"}, \code{"png"}, and \code{"jpg"}.
Default: \code{"svg"}.}

\item{file.name}{Optional character string giving the output file name when
\code{save = TRUE}. If \code{NULL}, the default file name is
\code{"plot_layer_distance_scale.<plot.type>"}. Default: \code{NULL}.}

\item{width}{A single positive numeric value giving the plot width in
centimeters when \code{save = TRUE}. Default: \code{16}.}

\item{height}{A single positive numeric value giving the plot height in
centimeters when \code{save = TRUE}. Default: \code{10}.}

\item{resolution}{A single numeric value giving the plot
resolution in dpi for \code{"png"} and \code{"jpg"} output. Default: \code{300}.}

\item{bottom.margin}{A single non-negative numeric value giving the bottom
plot margin in lines. Default: \code{3}.}

\item{left.margin}{A single non-negative numeric value giving the left plot
margin in lines. Default: \code{5}.}

\item{top.margin}{A single non-negative numeric value giving the top plot
margin in lines. Default: \code{2.5}.}

\item{right.margin}{A single non-negative numeric value giving the right plot
margin in lines. Default: \code{1}.}

\item{plot.title}{Optional character string giving the main plot title. If
\code{NULL}, no title is shown. Default: \code{"Layer distance scale across layers"}.}

\item{plot.title.font.size}{A single positive numeric value giving the plot
title font size in points. Default: \code{9.1}.}

\item{y.axis.label}{Optional character string giving the y-axis title. If
\code{NULL}, no y-axis title is shown. Default: \code{"Average pairwise distance"}.}

\item{axis.labels.font.size}{A single positive numeric value giving the
y-axis-title and x-axis layer-label font size in points. Default: \code{9.1}.}

\item{axis.ticks.font.size}{A single positive numeric value giving the
y-axis numeric tick-label font size in points. Default: \code{7}.}
}
\value{
Invisibly returns \code{NULL}. The function is called for its plotting
side effect. If \code{save = TRUE}, the plot is also written to the specified
file.
}
\description{
Plot the average pairwise distance scale of each input layer from a trained
multi-layer Self-Organizing Map (SOM). The function transforms the internal
distance-normalization weights stored for each replicate back to pairwise
distance scales, averages them across replicate maps, and displays the layers
from highest to lowest average distance.
}
\details{
\code{plot.layer.distance.scale.SOM} visualizes differences in the overall distance
scale among the input layers of a multi-layer SOM before internal weighting.
The plot should not be interpreted as a measure of layer importance but rather
as a diagnostic tool for assessing relative pairwise-distance scales among
layers before weighting.

The function takes the reciprocal of each replicate-specific entry in
\code{distance_weights_matrix} to recover the corresponding average pairwise
distance scale. These values are then averaged across replicate SOMs, and
layers are plotted from the highest to the lowest mean pairwise distance.

Relatively large values indicate layers that generate larger raw distances
before internal SOM distance normalization. Such values may reflect many
variables, large variance, sparsity, binary or SNP structure, or the selected
distance metric. Layers with larger raw distance scales receive smaller
internal distance-normalization weights, whereas layers with smaller raw
distance scales receive larger weights. This prevents differences in overall
distance magnitude from automatically determining the relative contribution
of each layer during SOM training.

Layer colors are assigned before the layers are reordered, so each color
remains linked to its original input layer. Bars have black borders, and
x-axis labels are shown in bold.

If \code{file.name = NULL}, the default file name is
\code{"plot_layer_distance_scale.<plot.type>"}.
}
\examples{
\dontrun{
set.seed(1)

# Create multi-layer example data
snp_data <- matrix(sample(0:2, 50 * 20, replace = TRUE),
                   nrow = 50,
                   ncol = 20)
morphology_data <- matrix(rnorm(50 * 5), nrow = 50, ncol = 5)
environment_data <- matrix(rnorm(50 * 4), nrow = 50, ncol = 4)
rownames(snp_data) <- paste0("sample_", seq_len(nrow(snp_data)))
rownames(morphology_data) <- rownames(snp_data)
rownames(environment_data) <- rownames(snp_data)
input_data_multi <- list(
  SNPs = snp_data,
  Morphology = morphology_data,
  Environment = environment_data
)

# Train multi-layer SOM
som_multi <- train.SOM(input_data = input_data_multi)

# Plot layer distance scales
plot.layer.distance.scale.SOM(SOM.output = som_multi)

# Save layer-distance-scale plot
plot.layer.distance.scale.SOM(
  SOM.output = som_multi,
  save = TRUE,
  plot.type = "svg",
  file.name = "SOM_layer_distance_scale.svg"
)
}

}
