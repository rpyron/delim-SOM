% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2026_04_07_delim-SOM_2.0_functions.R
\name{process.SNP.data.SOM}
\alias{process.SNP.data.SOM}
\title{Preprocess SNP data for SOM analysis}
\usage{
process.SNP.data.SOM(
  vcf.path = NULL,
  genind.input = NULL,
  genlight.input = NULL,
  snp.matrix.input = NULL,
  plink.raw.path = NULL,
  nexus.path = NULL,
  fasta.path = NULL,
  phylip.path = NULL,
  phylip.format = "sequential",
  plink.raw.metadata.columns = c("FID", "IID", "PAT", "MAT", "SEX", "PHENOTYPE"),
  alignment.ambiguity.mode = c("heterozygote", "missing"),
  snp.matrix.ploidy = 2,
  missing.loci.cutoff.lenient = 0.7,
  missing.loci.cutoff.final = 0.5,
  missing.individuals.cutoff = 0.5,
  singleton.loci.filter = TRUE,
  invariant.loci.filter = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{vcf.path}{Optional character string giving the path to a VCF file.
Default: \code{NULL}.}

\item{genind.input}{Optional \code{genind} object. Must contain codominant genetic
data with one uniform haploid or diploid ploidy value and valid unique
individual and locus names. Default: \code{NULL}.}

\item{genlight.input}{Optional \code{genlight} object. Must contain one uniform
haploid or diploid ploidy value. Ploidy must match \code{snp.matrix.ploidy}.
Default: \code{NULL}.}

\item{snp.matrix.input}{Optional numeric matrix or data frame containing SNP
dosages. Rows must represent samples and columns must represent SNP loci.
Values must be \code{0} and \code{1} for haploid data or \code{0}, \code{1}, and \code{2} for
diploid data, with missing values coded as \code{NA}. Default: \code{NULL}.}

\item{plink.raw.path}{Optional character string giving the path to a PLINK
\code{.raw} dosage file. Default: \code{NULL}.}

\item{nexus.path}{Optional character string giving the path to an aligned
NEXUS sequence file. Default: \code{NULL}.}

\item{fasta.path}{Optional character string giving the path to an aligned
FASTA sequence file. Default: \code{NULL}.}

\item{phylip.path}{Optional character string giving the path to an aligned
PHYLIP sequence file. Default: \code{NULL}.}

\item{phylip.format}{A single character string specifying the PHYLIP format.
Supported: \code{"sequential"} and \code{"interleaved"}. Default: \code{"sequential"}.}

\item{plink.raw.metadata.columns}{Character vector of column names to treat as
PLINK \code{.raw} metadata rather than SNP dosage columns. Default:
\code{c("FID", "IID", "PAT", "MAT", "SEX", "PHENOTYPE")}.}

\item{alignment.ambiguity.mode}{A single character string specifying handling
of two-base IUPAC ambiguity codes in sequence alignments. Supported:
\code{"heterozygote"} and \code{"missing"}. Recommended default: \code{"heterozygote"}.}

\item{snp.matrix.ploidy}{A single numeric value specifying the ploidy for
numeric dosage inputs. Must be \code{1} or \code{2}. Default: \code{2}.}

\item{missing.loci.cutoff.lenient}{A single numeric value between \code{0} and \code{1},
or \code{NULL}. Loci with a proportion of missing data greater than this value
are removed in the first lenient locus-level missing-data filter.
Recommended default: \code{0.7}.}

\item{missing.loci.cutoff.final}{A single numeric value between \code{0} and \code{1},
or \code{NULL}. Loci with a proportion of missing data greater than this value
are removed in the final stricter locus-level missing-data filter. This
value must be less than or equal to \code{missing.loci.cutoff.lenient} when both
are not \code{NULL}. Recommended default: \code{0.5}.}

\item{missing.individuals.cutoff}{A single numeric value between \code{0} and \code{1},
or \code{NULL}. Individuals with a proportion of missing data greater than this
value are removed. Recommended default: \code{0.5}.}

\item{singleton.loci.filter}{Logical; if \code{TRUE}, singleton loci are removed
using minor-allele count. Default: \code{TRUE}.}

\item{invariant.loci.filter}{Logical; if \code{TRUE}, invariant loci are removed
after missing-data and singleton filtering. Default: \code{TRUE}.}

\item{verbose}{Logical; if \code{TRUE}, filtering messages and summary are printed.
Default: \code{TRUE}.}
}
\value{
A data frame containing the processed SNP matrix, with samples in rows
and retained biallelic SNP variables in columns. Standard haploid data are
encoded as \code{0/1}, standard diploid genotype data are encoded as \code{0/1/2}, and
sequence alignment data are encoded on a \code{0/1/2} SNP dosage scale. Missing
values are retained as \code{NA}.
}
\description{
Read, filter, and process genetic marker data into a numeric SNP
matrix for Self-Organizing Map (SOM) analysis. The function accepts exactly
one genetic input source: a VCF file, \code{genind} object, \code{genlight} object,
numeric SNP dosage matrix, PLINK \code{.raw} file, or aligned NEXUS, FASTA,
or PHYLIP sequence file.
}
\details{
The output contains samples in rows and retained biallelic SNP variables in
columns. For standard diploid genotype inputs, values are encoded as \code{0},
\code{1}, or \code{2}, representing the retained allele-dosage coding. For standard
haploid dosage inputs, values are encoded as \code{0} or \code{1}. For sequence
alignment inputs, unambiguous biallelic sequence states are encoded as \code{0}
and \code{2} on the same SNP dosage scale. When
\code{alignment.ambiguity.mode = "heterozygote"}, matching two-base IUPAC
ambiguity codes are encoded as heterozygote dosage \code{1}. Missing genotypes or
sequence states are retained as \code{NA} and can be filtered by locus- and
individual-level missing-data thresholds.

VCF input is first processed with a fast parser for standard single-base
biallelic diploid SNPs. If the VCF genotype coding is not supported by the
fast parser, the function falls back to genind conversion when possible.
PLINK raw input is treated as diploid dosage data, so the declared SNP matrix
ploidy must be diploid. Standard PLINK metadata columns are removed before SNP
processing, and duplicated individual identifiers are resolved with
family-plus-individual identifiers when valid family identifiers are available.

For numeric SNP dosage matrices, row names are used as sample names and
column names are used as SNP names. Missing names are generated
automatically, but existing row and column names must be unique, non-missing,
and non-empty. For genlight input, the stored ploidy must be uniformly
haploid or diploid and must match the declared SNP matrix ploidy. For genind
input, the object must contain codominant data with one uniform haploid or
diploid ploidy value.

The function is intentionally biallelic-only. Multiallelic loci are removed
rather than expanded into K - 1 allele-dosage variables. This avoids
reference-dependent multiallelic encodings and prevents one biological locus
from contributing multiple SOM variables. This is important because K - 1
encodings can distort distance calculations and can create
reference-dependent behavior for binary or Tanimoto-style comparisons.

Sequence alignment inputs are treated as sequence-state alignments, with an
optional interpretation of two-base IUPAC ambiguity codes as diploid
heterozygotes. With \code{alignment.ambiguity.mode = "heterozygote"}, the function
assumes that \code{R}, \code{Y}, \code{S}, \code{W}, \code{K}, and \code{M} represent true heterozygous
genotypes when they match the two alleles observed or implied at a biallelic
site. For example, at an A/G site, \code{A} is encoded as \code{0}, \code{R} as \code{1}, and
\code{G} as \code{2}. With \code{alignment.ambiguity.mode = "missing"}, these ambiguity
codes are treated as missing values instead. The \code{"missing"} mode should be
used when ambiguity codes represent uncertain base calls, low-quality sequence
reads, mixed templates, or unresolved consensus states rather than true
heterozygosity.

The function applies the following general filtering order:
\enumerate{
\item Remove non-biallelic loci.
\item Remove loci exceeding the lenient missing-data threshold.
\item Remove individuals exceeding the missing-data threshold.
\item Optionally remove singleton loci using minor-allele count.
\item Remove loci exceeding the final missing-data threshold.
\item Optionally remove invariant loci.
}

Singleton filtering uses minor allele or sequence-state counts among
non-missing calls. Diploid dosage data and sequence alignment data with
\code{alignment.ambiguity.mode = "heterozygote"} are evaluated on the
called-chromosome scale. Standard haploid dosage data and sequence alignment
data with \code{alignment.ambiguity.mode = "missing"} are evaluated on the
observed-call scale. Invariant loci are removed only after missing-data and
singleton filtering because earlier filters can make previously variable loci
invariant.

For sequence alignment data with \code{alignment.ambiguity.mode = "heterozygote"},
biallelic-site detection includes both unambiguous nucleotide states and
alleles implied by two-base IUPAC ambiguity codes. An ambiguity code is
encoded as \code{1} only when it matches the two alleles at the retained
biallelic site. Singleton filtering in this mode uses a diploid allele-count
scale, so a \code{1} heterozygote contributes one alternate allele.

Missing sequence symbols, gaps, \code{N}, and non-two-base ambiguity codes such as
\code{B}, \code{D}, \code{H}, \code{V}, and \code{X} are treated as missing. In NEXUS alignments,
match characters are expanded from the first sequence before SNP encoding.
Multiallelic sites, polyploid data, mixed-ploidy data, and multiallelic
dosage expansion are not supported.
}
\examples{
\dontrun{
set.seed(1)

# Create example diploid SNP dosage matrix
snp_matrix <- matrix(
  sample(c(0, 1, 2, NA),
         50 * 100,
         replace = TRUE,
         prob = c(0.45, 0.25, 0.25, 0.05)),
  nrow = 50,
  ncol = 100
)
rownames(snp_matrix) <- paste0("sample_", seq_len(nrow(snp_matrix)))
colnames(snp_matrix) <- paste0("snp_", seq_len(ncol(snp_matrix)))

# Process diploid SNP dosage matrix
snp_layer <- process.SNP.data.SOM(
  snp.matrix.input = snp_matrix,
  missing.loci.cutoff.lenient = 0.7,
  missing.loci.cutoff.final = 0.5,
  missing.individuals.cutoff = 0.5
)

# Process aligned FASTA file
fasta_layer <- process.SNP.data.SOM(fasta.path = "alignment.fasta")

# Process PLINK .raw dosage file
plink_layer <- process.SNP.data.SOM(plink.raw.path = "genotypes.raw")
}

}
